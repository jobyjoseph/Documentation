#TDD

- `sudo n stable` -> Install stable version of node
- `sudo npm install -g create-react-app` -> Install create-react-app
- `create-react-app <app-name>`
- `cd <app-name>`
- `npm install enzyme jest-cli --save-dev`
- `npm install react-bootstrap --save`
- Create `App.test.js`
- `npm run test`
- `import { shallow } from "enzyme";`
- `npm i enzyme-adapter-react-16 --save-dev`
- create polyfill for requestAnimationFrame
- Implement enzyme adapter for React 16
- Create `App.js`