import SEO from "../../components/seo.js";
import Layout from "../../components/layout.js";
import JSLayout from "../../components/masters/javascript-layout.js";

<Layout>
<SEO title="if-else in JavaScript" />
<JSLayout>

# if-else

`if...else` in JavaScript helps us to make decisions.

```javascript
if (age > 60) {
  console.log("Senior");
} else {
  console.log("Not Senior");
}
```

</JSLayout>
</Layout>
