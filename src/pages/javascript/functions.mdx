import SEO from "../../components/seo.js";
import Layout from "../../components/layout.js";
import JSLayout from "../../components/masters/javascript-layout.js";

<Layout>
<SEO title="Functions in JavaScript" />
<JSLayout>

# Functions

A function is a JavaScript procedure. It means, function is a set of statements that performs a task or calculates a value.

Here is an example of a function.

```javascript
function displayName() {
  console.log("Backbencher");
}
```



</JSLayout>
</Layout>
